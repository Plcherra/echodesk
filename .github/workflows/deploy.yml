name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install SSH key
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Validate SSH configuration
        run: |
          test -n "${{ secrets.SSH_HOST }}" || { echo "SSH_HOST secret is empty"; exit 1; }
          test -n "${{ secrets.SSH_USER }}" || { echo "SSH_USER secret is empty"; exit 1; }
          test -n "${{ secrets.APP_PATH }}" || { echo "APP_PATH secret is empty"; exit 1; }

      - name: Add known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -T 30 -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Deploy to VPS
        env:
          # Gate deploy skips: set vars to 'true' when infra is ready.
          # ENABLE_NGINX_SYNC: set when passwordless sudo is configured (setup-passwordless-sudo.sh)
          # ENABLE_VALIDATE_INFRA: set when nginx/Telnyx/cloudflared are production-ready
          # ENABLE_GOOGLE_TTS_VALIDATION: set when VPS .env has GOOGLE_APPLICATION_CREDENTIALS
          ENABLE_NGINX_SYNC: ${{ vars.ENABLE_NGINX_SYNC || '' }}
          ENABLE_VALIDATE_INFRA: ${{ vars.ENABLE_VALIDATE_INFRA || '' }}
          ENABLE_GOOGLE_TTS_VALIDATION: ${{ vars.ENABLE_GOOGLE_TTS_VALIDATION || '' }}
        run: |
          ssh "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" "
            export ENABLE_NGINX_SYNC='$ENABLE_NGINX_SYNC'
            export ENABLE_VALIDATE_INFRA='$ENABLE_VALIDATE_INFRA'
            export ENABLE_GOOGLE_TTS_VALIDATION='$ENABLE_GOOGLE_TTS_VALIDATION'
            echo '=== Connected to VPS ==='
            cd ${{ secrets.APP_PATH }} || { echo 'Directory not found'; exit 1; }
            pwd
            git fetch --all
            git reset --hard origin/main
            git pull origin main || { echo 'Git pull failed'; exit 1; }
            echo 'Latest commit: '\$(git rev-parse HEAD)
            [ \"\$ENABLE_NGINX_SYNC\" != 'true' ] && export SKIP_NGINX_SYNC=1 || true
            [ \"\$ENABLE_VALIDATE_INFRA\" != 'true' ] && export SKIP_VALIDATE_INFRA=1 || true
            [ \"\$ENABLE_GOOGLE_TTS_VALIDATION\" != 'true' ] && export SKIP_GOOGLE_TTS_VALIDATION=1 || true
            # Always sync marketing + /auth/callback handoff page (email confirm → app).
            export DEPLOY_LANDING=1
            if [ -x ./deploy/scripts/deploy-systemd.sh ]; then
              bash ./deploy/scripts/deploy-systemd.sh || { echo 'Deploy failed'; exit 1; }
            else
              ./deploy/scripts/deploy.sh || { echo 'Deploy failed'; exit 1; }
            fi
            echo '=== Deployment completed ==='
          "
